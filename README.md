# Hidden Pre-Algebra Course Cloud v2.1

Student-facing design: unchanged original Pre-Algebra portal.

The cloud is entirely hidden infrastructure. It contains the expanded course snapshot and resolves internal directory/file routes at runtime. Directory links such as `diagnostic/` and `units/unit-01/` automatically resolve to their embedded `index.html`.

Use this build on the Dummy repository for testing before production migration.
